import DashboardLayout from "../_components/dashboard-layout";

export default function AdminPage() {
  return <DashboardLayout tableTitle="Klodran" villageTotal={50} stuntingTotal={30} normalTotal={60} userTotal={90}/>;
}
